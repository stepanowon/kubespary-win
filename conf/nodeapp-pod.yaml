apiVersion: v1
kind: Pod
metadata: 
  name: nodeapp
  labels: 
    app: nodeapp
spec:
  containers:
  - image: stepanowon/nodeapp:1.0.0
    name: nodeapp
    imagePullPolicy: Always
    ports: 
    - containerPort: 8080
      protocol: TCP
